---
"@veridtools/dmn-parser": minor
---

Add `outputLabel` to `OutputClause`, `isCollection` to `InputData`, `ImportedValues` interface with `LiteralExpression.importedValues`, fix `PerformanceIndicator.URI` casing, and expand `KnowledgeSource.type` to read child element form. Update `@veridtools/dmn-fixtures` to 0.3.0.
